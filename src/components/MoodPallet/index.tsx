import { Circle, Flex } from "@chakra-ui/react";
import React from "react";

export default function MoodPallet() {
    return (
        <Flex flexDirection='column'>
            <Circle bg='red' />
        </Flex>
    );
}
